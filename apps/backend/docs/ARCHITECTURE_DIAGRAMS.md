# Vertical Scaling Architecture Diagrams

## System Architecture

### Before Vertical Scaling (Single Instance)
```
┌─────────────────────────────────────────────────┐
│                   Server                        │
│                                                 │
│  ┌──────────────────────────────────────┐      │
│  │     Node.js Process                  │      │
│  │                                      │      │
│  │  • Uses only 1 CPU core              │      │
│  │  • Limited throughput                │      │
│  │  • No failover                       │      │
│  │                                      │      │
│  └──────────────┬───────────────────────┘      │
│                 │                               │
│                 ▼                               │
│  ┌──────────────────────────────────────┐      │
│  │         PostgreSQL                   │      │
│  └──────────────────────────────────────┘      │
│                                                 │
└─────────────────────────────────────────────────┘

Performance: ~1,000 req/sec, 25% CPU usage
```

### After Vertical Scaling (Cluster Mode)
```
┌─────────────────────────────────────────────────────────┐
│                   Server (4 cores)                      │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │         Master Process (Cluster Manager)        │   │
│  │  • Monitors workers                             │   │
│  │  • Restarts failed workers                      │   │
│  │  • Manages lifecycle                            │   │
│  └───┬─────────┬─────────┬─────────┬───────────────┘   │
│      │         │         │         │                   │
│  ┌───▼────┐┌───▼────┐┌───▼────┐┌───▼────┐             │
│  │Worker 1││Worker 2││Worker 3││Worker 4│             │
│  │        ││        ││        ││        │             │
│  │Core 1  ││Core 2  ││Core 3  ││Core 4  │             │
│  │512MB   ││512MB   ││512MB   ││512MB   │             │
│  └───┬────┘└───┬────┘└───┬────┘└───┬────┘             │
│      │         │         │         │                   │
│      └─────────┴─────────┴─────────┘                   │
│                   │                                     │
│                   ▼                                     │
│  ┌──────────────────────────────────────┐              │
│  │   PostgreSQL + Redis                 │              │
│  │   • Shared across all workers        │              │
│  │   • Connection pooling               │              │
│  └──────────────────────────────────────┘              │
│                                                         │
└─────────────────────────────────────────────────────────┘

Performance: ~3,500 req/sec, 90%+ CPU usage
```

## Request Flow

### Cluster Mode Request Handling
```
Client Request
     │
     ▼
┌─────────────────┐
│  Load Balancer  │  (OS/Kernel level)
│   (Round Robin) │
└────────┬────────┘
         │
    ┌────┴─────┬────────┬────────┐
    ▼          ▼        ▼        ▼
┌────────┐┌────────┐┌────────┐┌────────┐
│Worker 1││Worker 2││Worker 3││Worker 4│
│        ││        ││        ││        │
│ Handle ││ Handle ││ Handle ││ Handle │
│Request ││Request ││Request ││Request │
└───┬────┘└───┬────┘└───┬────┘└───┬────┘
    │         │         │         │
    └─────────┴─────────┴─────────┘
              │
              ▼
      ┌──────────────┐
      │  PostgreSQL  │
      │    Redis     │
      └──────────────┘
              │
              ▼
         Response
```

## Worker Lifecycle

### Normal Operation
```
Master Process
     │
     ├─ Fork Worker 1 ──> 💚 Online ──> 🟢 Handling Requests
     │
     ├─ Fork Worker 2 ──> 💚 Online ──> 🟢 Handling Requests
     │
     ├─ Fork Worker 3 ──> 💚 Online ──> 🟢 Handling Requests
     │
     └─ Fork Worker 4 ──> 💚 Online ──> 🟢 Handling Requests
```

### Worker Failure and Recovery
```
Worker 2 Crashes
     │
     ▼
⚠️  Worker 2 died
     │
     ▼
Master detects failure
     │
     ▼
🔄 Fork new Worker 2
     │
     ▼
💚 New Worker 2 online
     │
     ▼
🟢 Handling Requests

Note: Other workers continue working during recovery
```

## Memory Distribution

### Single Instance (4GB RAM)
```
┌─────────────────────────────────┐
│     Node.js Process (1 core)   │
│                                 │
│  Heap: ~1.4GB (default)         │
│  Used: Variable                 │
│  Wasted: ~2.6GB unused          │
│                                 │
└─────────────────────────────────┘
```

### Cluster Mode (4GB RAM, 4 workers)
```
Master Process (100MB)
┌──────────────┐
│   Master     │  100MB
└──────────────┘

Workers (950MB each = 3.8GB)
┌──────────────┐
│   Worker 1   │  950MB
├──────────────┤
│   Worker 2   │  950MB
├──────────────┤
│   Worker 3   │  950MB
├──────────────┤
│   Worker 4   │  950MB
└──────────────┘

System Reserve: 100MB
Total: ~4GB (fully utilized)
```

## Scaling Comparison

### Resource Utilization
```
Single Instance          Cluster Mode (4 cores)
━━━━━━━━━━━━━━━━        ━━━━━━━━━━━━━━━━━━━━━━

CPU Usage:              CPU Usage:
Core 1: ████ 100%       Core 1: ████ 95%
Core 2: ░░░░   0%       Core 2: ████ 93%
Core 3: ░░░░   0%       Core 3: ████ 97%
Core 4: ░░░░   0%       Core 4: ████ 91%
Average: 25%            Average: 94%

Memory:                 Memory:
Used:   ███░ 35%        Used:   ████ 95%
Free:   ░░░░ 65%        Free:   ░░░░  5%

Throughput:             Throughput:
1,000 req/sec           3,500 req/sec

Latency (p95):          Latency (p95):
50ms                    35ms
```

## Docker Deployment Architecture

### Development Setup
```
┌─────────────────────────────────────────┐
│         docker-compose.yml              │
│                                         │
│  ┌──────────────────┐                  │
│  │   PostgreSQL     │                  │
│  │   Port: 5432     │                  │
│  │   Limit: 2 CPU   │                  │
│  │          2GB RAM │                  │
│  └──────────────────┘                  │
│                                         │
│  Backend runs on host (pnpm dev)       │
│                                         │
└─────────────────────────────────────────┘
```

### Production Setup
```
┌──────────────────────────────────────────────────────┐
│         docker-compose.prod.yml                      │
│                                                      │
│  ┌──────────────────┐  ┌──────────────────┐         │
│  │   PostgreSQL     │  │     Redis        │         │
│  │   Port: 5432     │  │   Port: 6379     │         │
│  │   Limit: 2 CPU   │  │   Limit: 1 CPU   │         │
│  │          4GB RAM │  │          1GB RAM │         │
│  └────────┬─────────┘  └────────┬─────────┘         │
│           │                     │                   │
│           └─────────┬───────────┘                   │
│                     │                               │
│           ┌─────────▼──────────┐                    │
│           │   Backend (Cluster)│                    │
│           │   Port: 3001       │                    │
│           │   Workers: 4       │                    │
│           │   Limit: 4 CPU     │                    │
│           │          8GB RAM   │                    │
│           └────────────────────┘                    │
│                                                      │
└──────────────────────────────────────────────────────┘

Total Resources: 7 CPU, 13GB RAM
```

## Performance Metrics Over Time

### Single Instance
```
Requests/sec
1500 │
     │
1000 │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ ← Bottleneck
 500 │
   0 └─────────────────────────────
     0    5   10   15   20   (time)
```

### Cluster Mode (4 workers)
```
Requests/sec
4000 │               ▓▓▓▓▓▓▓▓
3500 │           ▓▓▓▓
3000 │       ▓▓▓▓          ← Linear scaling
2000 │   ▓▓▓▓
1000 │▓▓▓
   0 └─────────────────────────────
     0    5   10   15   20   (time)
```

## Graceful Shutdown Flow

```
SIGTERM/SIGINT received
         │
         ▼
Master Process
         │
         ├─> Send "shutdown" to Worker 1
         │   └─> Worker 1: Finish current requests (5s)
         │       └─> Worker 1 exits
         │
         ├─> Send "shutdown" to Worker 2
         │   └─> Worker 2: Finish current requests (5s)
         │       └─> Worker 2 exits
         │
         ├─> Send "shutdown" to Worker 3
         │   └─> Worker 3: Finish current requests (5s)
         │       └─> Worker 3 exits
         │
         └─> Send "shutdown" to Worker 4
             └─> Worker 4: Finish current requests (5s)
                 └─> Worker 4 exits

All workers exited
         │
         ▼
Master Process exits
         │
         ▼
Clean shutdown complete ✅
```

## Cost Comparison

### Monthly Costs (Example)

#### Horizontal Scaling (Old Approach)
```
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  Server 1   │  │  Server 2   │  │  Server 3   │
│  2 cores    │  │  2 cores    │  │  2 cores    │
│  4GB RAM    │  │  4GB RAM    │  │  4GB RAM    │
│  $30/month  │  │  $30/month  │  │  $30/month  │
└─────────────┘  └─────────────┘  └─────────────┘
         Total: $90/month
         Capacity: ~3,000 req/sec
```

#### Vertical Scaling (New Approach)
```
┌─────────────────────┐
│     Server 1        │
│     6 cores         │
│     12GB RAM        │
│     $60/month       │
│                     │
│   Cluster Mode:     │
│   6 workers         │
│   Full utilization  │
└─────────────────────┘
   Total: $60/month
   Capacity: ~5,000 req/sec

   Savings: $30/month (33%)
   Better performance per $
```

## When to Scale Horizontally

### Traffic Thresholds
```
0-5k req/min     ├─────── Vertical Scaling ────────┤
                 Single server with clustering

5k-50k req/min   ├──── Vertical + Basic Horizontal ────┤
                 1-3 servers with load balancer

50k+ req/min     ├───── Full Horizontal Scaling ─────┤
                 Multiple servers + auto-scaling
```

### Decision Matrix
```
                    Vertical          Horizontal
                    Scaling           Scaling
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Cost               ⭐⭐⭐⭐⭐         ⭐⭐⭐
Complexity         ⭐⭐⭐⭐⭐         ⭐⭐
Max Capacity       ⭐⭐⭐            ⭐⭐⭐⭐⭐
High Availability  ⭐⭐              ⭐⭐⭐⭐⭐
Geo Distribution   ⭐                ⭐⭐⭐⭐⭐
Maintenance        ⭐⭐⭐⭐⭐         ⭐⭐⭐
```

## Legend

- 🟢 Active/Online
- ⚠️ Warning/Issue
- ❌ Error/Offline
- 💚 Healthy
- 🔄 Restarting
- ✅ Success
- ⭐ Rating (5 stars = best)
